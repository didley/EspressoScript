# Dependencies
node_modules/

# Build output — not committed; built on publish via `pnpm build`
dist/

# Package manager lock files — pnpm-lock.yaml is committed; others are not
bun.lock
yarn.lock

# Generated site docs (built from source markdown at deploy time)
site/docs/*.md

# Editor / OS artifacts
.DS_Store
*.local

# tsx/esm build artifacts (bare module names at repo root)
/glob
/typescript
/node:*
