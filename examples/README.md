# ShotScript Examples

## `hello-world/`

Minimal project showing ShotScript setup — tsconfig plugin, Biome formatter config, and a simple typed function using the result-tuple pattern.

```bash
cd hello-world
npm install
npm run lint
```
