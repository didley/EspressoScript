#!/usr/bin/env bash
set -euo pipefail

ROOT="$(cd "$(dirname "$0")/.." && pwd)"

echo "shot pre-commit: running fixture tests..."
bun run "$ROOT/tests/run-syntax-fixtures.ts"
bun run "$ROOT/tests/run-types-fixtures.ts"
bun run "$ROOT/tests/run-imports-fixtures.ts"
echo "shot pre-commit: all checks passed."
